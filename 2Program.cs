using System;

namespace ConsoleApp.SQLite
{
    public class Progra
    {
        /*
        public static void Mai()
        {
            using (var db = new Cinema())
            {
                db.Categorias.Add(new Categoria { Descricao = "Descricao linda aqui",  });
                var count = db.SaveChanges();
                Console.WriteLine("Salvo com sucesso");

                Console.WriteLine();
                Console.WriteLine("Todas as Descrições:");
                foreach (var categoria in db.Categorias)
                {
                    Console.WriteLine(categoria.CategoriaId + " - " + categoria.Descricao);
                }
            }
        }
        */
    }
}